# Predict-A-Pool Infrastructure

- The terraform scripts will create a ECS Cluster and deploy the backend and frontend as an ECS Service
- The deployments are Docker images stored in ECR
- We are using Fargate to keep things as portable as possible